<h1><%= @category.name %></h1>

<% @category.books.each do |book| %>
<%= link_to (image_tag(book['large_image_url'])), book['item_url'] %>
<%= book.title %>
<%= book.author %>
<%= book.publisher_name %>
<%= link_to "レビュー詳細を見る", review_path(book) %>
<% end %>

